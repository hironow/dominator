# NFR check request from amadeus

This is a synthetic D-Mail fixture used by the dominator jun15 MCP
pivot contract test (refs/issues/0027 §8). It exercises the
symmetric 9-field envelope schema in the **amadeus → dominator**
direction (= dominator as NFR check executor), complementing
paintress's, sightjack's, and amadeus's own fixtures.

## Request

- Target NFR: `api-latency-p95`
- Trigger: PR #42 merged to main (auto-merge gate)
- Expected: p95 latency < 200ms under 100 RPS for 60s

(This body is intentionally short; real NFR check requests are
larger.)
